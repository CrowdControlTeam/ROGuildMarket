// Nada que mostrar por defecto — el slot solo tiene contenido cuando la
// navegación interceptada a (.)[id] está activa (ver ../(.)[id]/page.tsx).
export default function Default() {
  return null;
}
